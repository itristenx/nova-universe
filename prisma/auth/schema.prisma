datasource auth_db {
  provider = "postgresql"
  url      = env("AUTH_DATABASE_URL")
}

generator authClient {
  provider   = "prisma-client-js"
  datasource = "auth_db"
  output     = "../generated/auth"
}
