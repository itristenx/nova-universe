# Nova Universe Environment Configuration
# Copy this file to .env and customize your settings

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
PORT=3000
NODE_ENV=d# =============================================================================
# AI & COSMO CONFIGURATION
# =============================================================================

# Nova Synth AI Engine Configuration
SYNTH_API_URL=http://localhost:3001
SYNTH_API_KEY=synth_api_key_change_me_in_production
SYNTH_WEBSOCKET_URL=ws://localhost:3001/ws
SYNTH_MCP_ENDPOINT=http://localhost:3001/mcp

# Cosmo Assistant Configuration
COSMO_ENABLED=true
COSMO_MODEL_PROVIDER=openai
COSMO_MODEL_NAME=gpt-4o-mini
COSMO_PERSONALITY=friendly_professional
COSMO_MAX_CONTEXT_LENGTH=8000
COSMO_SESSION_TIMEOUT=3600000
COSMO_XP_ENABLED=true

# AI Model Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here
ANTHROPIC_BASE_URL=https://api.anthropic.com

# Alternative Model Providers
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_DEPLOYMENT_NAME=
GOOGLE_AI_API_KEY=
COHERE_API_KEY=

# Model Context Protocol (MCP) Settings
MCP_ENABLED=true
MCP_SERVER_TIMEOUT=30000
MCP_MAX_CONCURRENT_SESSIONS=100
MCP_SESSION_CLEANUP_INTERVAL=300000

# AI Ticket Processing Configuration
AI_TICKET_PROCESSING_ENABLED=true
AI_AUTO_CLASSIFY_CATEGORY=true
AI_AUTO_CLASSIFY_PRIORITY=true
AI_AUTO_MATCH_CUSTOMERS=true
AI_DUPLICATE_DETECTION_ENABLED=true
AI_DUPLICATE_THRESHOLD=0.8
AI_SIMILARITY_THRESHOLD=0.6
AI_TREND_ANALYSIS_ENABLED=true
AI_PROCESSING_QUEUE_SIZE=1000
AI_PROCESSING_BATCH_SIZE=10
AI_CUSTOMER_MATCHING_CONFIDENCE_THRESHOLD=0.8

# Machine Learning Models
ML_TICKET_CLASSIFIER_MODEL=distilbert-base-uncased
ML_SIMILARITY_MODEL=all-MiniLM-L6-v2
ML_SENTIMENT_ANALYSIS_ENABLED=true
ML_LANGUAGE_DETECTION_ENABLED=true
ML_KEYWORD_EXTRACTION_ENABLED=true

# Local AI/ML Processing (for offline capabilities)
LOCAL_AI_ENABLED=false
LOCAL_AI_MODEL_PATH=/var/lib/nova/models
HUGGINGFACE_API_KEY=hf_your_huggingface_token_here
HUGGINGFACE_MODEL_CACHE=/tmp/huggingface_cache

# Conversation & Context Management
REDIS_CONVERSATION_TTL=86400
CONVERSATION_HISTORY_LIMIT=50
CONTEXT_WINDOW_SIZE=32000

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment
NODE_ENV=development

# Server settings
PORT=3000
HOST=localhost

# Organization settings
ORGANIZATION_NAME=Nova Universe
LOGO_URL=/logo.png
FAVICON_URL=/vite.svgSECRET=your-super-secure-jwt-secret-key-here
PUBLIC_URL=http://localhost:3000
API_URL=http://localhost:3000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Primary database type: postgresql, mongodb
# For hybrid setup, specify: postgresql,mongodb,elasticsearch
PRIMARY_DATABASE=postgresql,mongodb,elasticsearch

# PostgreSQL Configuration (Core Data with Prisma ORM)
# Connection details for the core database
CORE_DATABASE_URL=postgresql://nova_user:secure_password_here@localhost:5432/nova_core
CORE_DB_HOST=localhost
CORE_DB_PORT=5432
CORE_DB_NAME=nova_core
CORE_DB_USER=nova_user
# PostgreSQL password (required)
CORE_DB_PASSWORD=secure_password_here

# Connection details for the auth database
AUTH_DATABASE_URL=postgresql://nova_user:secure_password_here@localhost:5432/nova_auth
AUTH_DB_HOST=localhost
AUTH_DB_PORT=5432
AUTH_DB_NAME=nova_auth
AUTH_DB_USER=nova_user
AUTH_DB_PASSWORD=secure_password_here

# PostgreSQL Connection Pool
POSTGRES_MAX_CONNECTIONS=20
POSTGRES_MIN_CONNECTIONS=2
POSTGRES_IDLE_TIMEOUT=30000
POSTGRES_CONNECTION_TIMEOUT=5000

# PostgreSQL SSL Configuration (production)
POSTGRES_SSL=false
POSTGRES_SSL_CERT_PATH=
POSTGRES_SSL_KEY_PATH=
POSTGRES_SSL_CA_PATH=

# Database Logging Configuration
# Set to 'true' to enable verbose database connection/query logging (production default)
# Set to 'false' for quieter development experience
DB_VERBOSE_LOGGING=false

# =============================================================================
# FILE STORAGE CONFIGURATION
# =============================================================================

# Storage Type: 'local', 's3', or 'hybrid'
# - 'local': Store all files locally
# - 's3': Store all files in S3
# - 'hybrid': Intelligent routing based on file type/context (recommended)
STORAGE_TYPE=hybrid

# Local Storage Configuration
LOCAL_STORAGE_BASE_PATH=./uploads
LOCAL_STORAGE_BASE_URL=/uploads

# S3 Storage Configuration (optional but recommended for hybrid mode)
# S3 is used for large attachments and backups in hybrid mode
AWS_S3_BUCKET=nova-universe-assets
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-key-here
AWS_S3_ENDPOINT=
AWS_S3_FORCE_PATH_STYLE=false

# Hybrid Storage Rules (optional - smart defaults provided)
# Specify which file types/contexts use which storage provider
ATTACHMENTS_STORAGE=s3  # ticket attachments prefer S3
PROFILE_IMAGES_STORAGE=local  # profile images stay local for speed

# File Upload Limits
UPLOAD_MAX_FILE_SIZE=10485760
UPLOAD_ALLOWED_TYPES=jpeg,jpg,png,gif,svg,ico,pdf,txt,json,zip,doc,docx,xls,xlsx,ppt,pptx

# MongoDB Configuration for audit logging
AUDIT_DATABASE_URL=mongodb://localhost:27017/nova_audit
AUDIT_DB_USER=nova_user
AUDIT_DB_PASSWORD=secure_password_here

# MongoDB Options
MONGO_AUTH_SOURCE=admin
MONGO_REPLICA_SET=
MONGO_SSL=false
MONGO_SSL_CERT_PATH=
MONGO_SSL_KEY_PATH=
MONGO_SSL_CA_PATH=

# Elasticsearch Configuration (Search and Analytics)
ELASTIC_URL=http://localhost:9200
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=changeme
ELASTIC_API_KEY=
ELASTIC_CLOUD_ID=
ELASTIC_SSL_VERIFY=false

# Elasticsearch Index Settings
ELASTIC_TICKETS_INDEX=nova_tickets
ELASTIC_LOGS_INDEX=nova_logs
ELASTIC_KB_INDEX=nova_kb
ELASTIC_AI_CONTEXT_INDEX=nova_ai_context

# Elasticsearch Performance Settings
ELASTIC_REQUEST_TIMEOUT=30000
ELASTIC_PING_TIMEOUT=3000
ELASTIC_MAX_RETRIES=3
ELASTIC_COMPRESSION=false
ELASTIC_SNIFF_ON_START=false
ELASTIC_SNIFF_INTERVAL=
ELASTIC_SNIFF_ON_CONNECTION_FAULT=false

# Redis Configuration (for caching and sessions)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Admin User Configuration
ADMIN_EMAIL=admin@example.com
ADMIN_NAME=Administrator
ADMIN_PASSWORD=admin123!

# Session Configuration
SESSION_SECRET=your-session-secret-key-here
SESSION_MAX_AGE=86400000

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# Password Security
MIN_PIN_LENGTH=4
MAX_PIN_LENGTH=8

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================

# SMTP Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Microsoft 365 / Graph Settings
M365_CLIENT_ID=
M365_CLIENT_SECRET=
M365_TENANT_ID=
M365_GRAPH_SCOPES=Mail.ReadWrite Mail.Send MailboxSettings.Read User.Read.All
M365_WEBHOOK_SECRET=random_string_here
M365_POLL_INTERVAL_MS=60000

# Email Templates
FROM_EMAIL=noreply@yourorg.com
FROM_NAME=Your Organization

# =============================================================================
# ORGANIZATION BRANDING
# =============================================================================

ORGANIZATION_NAME=Your Organization
LOGO_URL=/logo.png
FAVICON_URL=/vite.svg

# Welcome Messages
WELCOME_MESSAGE=Welcome to the Help Desk
HELP_MESSAGE=Need to report an issue?

# Status Messages
STATUS_OPEN_MSG=Open
STATUS_CLOSED_MSG=Closed
STATUS_ERROR_MSG=Error
STATUS_MEETING_MSG=In a Meeting - Back Soon
STATUS_BRB_MSG=Be Right Back
STATUS_LUNCH_MSG=Out to Lunch - Back in 1 Hour
STATUS_UNAVAILABLE_MSG=Status Unavailable

# =============================================================================
# DIRECTORY INTEGRATION
# =============================================================================

# SCIM Configuration
SCIM_TOKEN=your-scim-token-here

# Directory Provider Settings
DIRECTORY_ENABLED=false
DIRECTORY_PROVIDER=mock
DIRECTORY_URL=
DIRECTORY_TOKEN=

# =============================================================================
# BACKUP CONFIGURATION
# =============================================================================

# Backup Settings
BACKUP_RETENTION_DAYS=7
BACKUP_SCHEDULE=0 2 * * *

# =============================================================================
# MONITORING AND LOGGING
# =============================================================================

# Log Level: error, warn, info, verbose, debug, silly
LOG_LEVEL=info
LOG_FILE=server.log

# Health Check
HEALTH_CHECK_INTERVAL=30000

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Development Flags
CLI_MODE=false
DEBUG_SQL=false
ENABLE_QUERY_LOGGING=false

# Testing
TEST_DATABASE=true
TEST_RESET_DB=true

# =============================================================================
# PRODUCTION CONFIGURATION
# =============================================================================

# SSL/TLS Settings
ENABLE_HTTPS=false
SSL_CERT_PATH=
SSL_KEY_PATH=

# Cluster Mode
CLUSTER_MODE=false
CLUSTER_WORKERS=0

# External Services
EXTERNAL_API_TIMEOUT=5000
EXTERNAL_API_RETRIES=3
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=nova_universe
POSTGRES_USER=nova_admin
# PostgreSQL password (required)
POSTGRES_PASSWORD=secure_password_here

# SSL/TLS settings (enable for production)
POSTGRES_SSL_REJECT_UNAUTHORIZED=false
POSTGRES_SSL_CA=
POSTGRES_SSL_CERT=
POSTGRES_SSL_KEY=

# Connection pool settings
POSTGRES_POOL_MIN=2
POSTGRES_POOL_MAX=20
POSTGRES_POOL_ACQUIRE_TIMEOUT=60000
POSTGRES_POOL_IDLE_TIMEOUT=30000
POSTGRES_POOL_CREATE_TIMEOUT=30000
POSTGRES_POOL_DESTROY_TIMEOUT=5000
POSTGRES_POOL_REAP_INTERVAL=1000
POSTGRES_POOL_CREATE_RETRY_INTERVAL=200

# Query timeouts (milliseconds)
POSTGRES_STATEMENT_TIMEOUT=30000
POSTGRES_QUERY_TIMEOUT=30000
POSTGRES_CONNECTION_TIMEOUT=5000

# ==============================================================================
# MONGODB CONFIGURATION
# ==============================================================================

# Connection settings
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=nova_universe
MONGO_USERNAME=nova_admin
MONGO_PASSWORD=mongo_secure_pass_2024
MONGO_AUTH_SOURCE=admin
MONGO_AUTH_MECHANISM=SCRAM-SHA-256

# Connection pool settings
MONGO_MAX_POOL_SIZE=10
MONGO_MIN_POOL_SIZE=2
MONGO_MAX_IDLE_TIME=30000
MONGO_SERVER_SELECTION_TIMEOUT=5000
MONGO_SOCKET_TIMEOUT=45000
MONGO_CONNECT_TIMEOUT=10000
MONGO_HEARTBEAT_FREQUENCY=10000

# Debug settings
MONGO_DEBUG=false

# ==============================================================================
# REDIS CONFIGURATION (Optional)
# ==============================================================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_secure_pass_2024
REDIS_DB=0

# ==============================================================================
# DOCKER COMPOSE CONFIGURATION
# ==============================================================================

# PostgreSQL Admin (pgAdmin)
PGADMIN_EMAIL=admin@nova.local
PGADMIN_PASSWORD=admin123
PGADMIN_PORT=8080

# MongoDB Admin (mongo-express)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=mongo_secure_pass_2024
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=admin123
MONGO_EXPRESS_PORT=8081

# Backup settings
BACKUP_RETENTION_DAYS=7

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================

# Environment
NODE_ENV=development

# Server settings
PORT=3000
HOST=localhost

# Organization settings
ORGANIZATION_NAME=Nova Universe
LOGO_URL=/logo.png
FAVICON_URL=/vite.svg

# Security settings
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
ADMIN_EMAIL=admin@nova.local
ADMIN_NAME=System Administrator
ADMIN_PASSWORD=admin123

# Pin settings
MIN_PIN_LENGTH=4
MAX_PIN_LENGTH=8

# Email settings (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# SCIM settings
SCIM_TOKEN=

# ==============================================================================
# DEVELOPMENT/DEBUG SETTINGS
# ==============================================================================

# Disable certain logs in CLI mode
CLI_MODE=false

# Enable debug logging
DEBUG=false

# Log levels: error, warn, info, debug
LOG_LEVEL=info

# ==============================================================================
# PRODUCTION SECURITY NOTES
# ==============================================================================

# For production deployment:
# 1. Change all default passwords
# 2. Enable SSL/TLS for all database connections
# 3. Use strong, randomly generated secrets
# 4. Set NODE_ENV=production
# 5. Configure proper backup strategies
# 6. Enable audit logging
# 7. Set up monitoring and alerting
# 8. Use environment-specific configuration management
# Example of a 32-byte hexadecimal key (64 characters long):
# You can generate a key using: openssl rand -hex 32
ASSET_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

