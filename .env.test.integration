# Nova Universe - Integration Test Environment
NODE_ENV=test
PORT=3000
API_PORT=3000
FORCE_LISTEN=true
DISABLE_AUTH=true
CORS_ORIGINS=http://localhost:4101,http://localhost:4102

# Required secrets
SESSION_SECRET=test_session_secret_integration
JWT_SECRET=test_jwt_secret_integration
KIOSK_TOKEN=test_kiosk_token_integration
SCIM_TOKEN=test_scim_token_integration

# Database (compose provides DATABASE_URL; include explicit vars for fallback)
POSTGRES_HOST=localhost
POSTGRES_USER=nova_test
POSTGRES_PASSWORD=test_password_integration
AUTH_DB_PASSWORD=test_password_integration
POSTGRES_DB=nova_test
DATABASE_URL=postgresql://nova_test:test_password_integration@localhost:4132/nova_test

# Redis
REDIS_URL=redis://localhost:4179

# SMTP (optional for tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
HELPDESK_EMAIL=helpdesk@nova.test

# Feature flags
MONGO_ENABLED=false
HELPSCOUT_SMTP_FALLBACK=true