# Nova Universe - Default Test Environment
NODE_ENV=test
PORT=3000
API_PORT=3000
FORCE_LISTEN=true
DISABLE_AUTH=true
CORS_ORIGINS=http://localhost:4001,http://localhost:4002

# Required secrets
SESSION_SECRET=test_session_secret
JWT_SECRET=test_jwt_secret
KIOSK_TOKEN=test_kiosk_token
SCIM_TOKEN=test_scim_token

# Database
POSTGRES_HOST=localhost
POSTGRES_USER=nova_test
POSTGRES_PASSWORD=test_password_test
POSTGRES_DB=nova_test
DATABASE_URL=postgresql://nova_test:test_password_test@localhost:4032/nova_test

# Redis
REDIS_URL=redis://localhost:4079

# SMTP (optional for tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
HELPDESK_EMAIL=helpdesk@nova.test

# Feature flags
MONGO_ENABLED=false
HELPSCOUT_SMTP_FALLBACK=true